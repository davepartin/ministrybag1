# REV-001: review ENG-001 through ENG-003

Date: 2026-09-11
Agent / reviewer: Codex
Status: REVIEW, checks passed, pending integration
Branch: codex/review-eng-001-003
Reviewed base: 1a424704ed5cf293d5652f902000d3c92bafe17b
Scope: merged PRs #10, #11 and #12; bounded answer persistence and iframe sizing corrections, regression tests, board and handoff.

## Verdict

The small work packets, separate commits and synthetic checks are useful. All three implementations stayed within their engineering assignments. However, a passing helper test did not establish preservation of the actual stored bytes or real browser shrink behavior. Two missed edge cases needed repair before clearing the next worker. PR #12 is merged, not awaiting integration as the handoff previously stated.

ENG-001 and ENG-003 can clear after this corrective branch is integrated. ENG-002 has working assets and remains REVIEW for Dave's artwork and actual phone assessment. No lesson theology or editorial approval was added by this review. Three merged engineering packets do not mean three approved lessons.

## Corrections

1. Malformed JSON was replaced during startup migration with empty migration metadata. Reproduced with a synthetic truncated string: `malformed: true`, but the original bytes were lost. The loader now skips migration on malformed/unavailable reads. The host blocks all response writes for that failed-load session, including subsequent edits and periodic saves. A denied localStorage getter is also caught during save. Valid loads and normal migration remain writable.
2. Iframe measurement included its own current viewport height. A real browser fixture with 220 px of content in a 640 px iframe remained 640 px on the reviewed base. Measurement now temporarily removes that height floor, restores hidden/tiny measurements and sizes to the measured content. Observers can grow and shrink the frame without a loop. The previous synthetic shrink test missed the browser's viewport floor.
3. Added `scripts/review_eng_browser_check.js` for those real browser edge cases and four raw-storage preservation unit checks.

## Checks

All commands run from `discipleship/`, with a local server started using `python3 -m http.server 8765 --bind 127.0.0.1`.

- `node scripts/test_answer_storage.js`: 44 checks passed, including malformed bytes and failed reads remaining untouched.
- `node scripts/test_widget_iframe.js`: 30 checks passed.
- `node scripts/test_lesson_assets.js`: 8 checks passed.
- `bash scripts/qa_foundation.sh`: passed, including all-course unique runtime IDs and the asset/iframe checks.
- Existing browser scripts: `eng001_browser_check.js` (17 checks), `eng002_browser_check.js` (14 checks), `eng003_browser_check.js` (36 checks), all passed. These cover independent answers and reload, both image pairs, widget steps/back/restart, course switching, titles and reachable controls at phone and laptop widths.
- `review_eng_browser_check.js`: 14 checks passed, covering real shrink/grow at 375, 390 and 1280 px, observer stability, malformed and failed reads, repeated saves, preserved in-memory edits and a blocked storage getter.
- Inspected 201-08 widget screenshots at 375 and 1280 px. Existing card and Begin control remain intact; no excessive frame space. Inspected the two new color artwork files: recognizable subjects and consistent charcoal treatment. This is technical/art review evidence, not Dave's approval.

Local runtime detail: the default Playwright Chromium binary was unavailable. No package install was needed; checks ran in installed Chrome using the bundled Playwright package. Reproduce with:

```sh
export NODE_PATH=/Users/dpartin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules
GT_BROWSER_CHANNEL=chrome node scripts/review_eng_browser_check.js
node - <<'JS'
const { chromium } = require('playwright');
const launch = chromium.launch.bind(chromium);
chromium.launch = options => launch({ ...options, channel: 'chrome' });
require('./scripts/eng003_browser_check.js');
JS
```

Use `eng001_browser_check.js` or `eng002_browser_check.js` in the final require to rerun those checks. Other machines should use their installed Playwright package/browser paths.

## Remaining limits and next work

- A failed-load session intentionally cannot persist new edits. Those edits stay in memory only and will be lost on reload. ENG-005 must make this visible and provide a recovery path before learner release; this patch alone does not complete save-failure UX.
- Ambiguous historical answers are preserved in metadata. A learner-facing recovery/backup route remains ENG-006; do not silently assign a shared answer to a lesson.
- No actual iPhone/Safari, print, cross-device synchronization, production deployment or performance benchmark was verified here.
- Artwork approval remains with Dave. The foot-washing PNG pair is roughly 3 MiB; optimize through PERF-001 after approval.
- ENG-004 is next after integration: current lesson by default, explicit course option, visible preview, no automatic send. Then ENG-005 and SEC-001. Keep login deferred.
- Dave's writing and pilot work should proceed alongside the software queue. His two 202-08/09 pastoral briefs are still the highest-value curriculum input.

No real learner store was inspected. No credentials were copied into the review. No lesson content or image files changed.
