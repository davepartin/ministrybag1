# REV-004: review ENG-006a backup download

Date: September 12, 2026. Reviewer: Codex. Isolated worktree: `/tmp/discipleship-coordination-20260911`; branch: `codex/review-eng-006a`. Reviewed PR #19 starting at `0b78faec9c6f9899438cce243d9bf75ba60446f6`, based on main `8b1825ec`. Claude PR #17 was left open and its lesson files were not changed.

## Result and correction

The versioned download preserves all three current in-memory stores, stable IDs and retained ambiguous-answer metadata. Readable empty data and unreadable data remain distinguishable. Actual downloaded JSON preserves synthetic multiline/Unicode answers, checklists, commitments, completion and reading values. Download does not restore or upload data, replace stored data, or clear the failed-load guard.

Corrected one misleading status message: a partial backup previously always said an original recovery copy was kept, including a blocked read where no original bytes were available. The message now distinguishes captured original data from unavailable data and states that existing browser data was not replaced. Added unit assertions and a real downloaded-file browser case for inaccessible answers with unsaved edits. Also restricted synthetic seeding to the top frame so widget initialization cannot reset test stores.

## Independently run checks

- `node scripts/test_backup_download.js`: 53 passed after correction (original 51 also passed).
- `node scripts/test_save_feedback.js`: 41 passed.
- `node scripts/test_answer_storage.js`: 44 passed.
- `bash scripts/qa_foundation.sh`: all passed, including the export and updated backup unit checks.
- `node scripts/eng006a_browser_check.js`: all 60 browser assertions passed, including actual downloaded bytes, keyboard Enter, empty data, malformed originals, blocked reads and unsaved edits; viewport checks at 375, 390 and 1280 pixels.
- `node scripts/review_save_recovery_browser_check.js`: 20 passed; multi-store recovery, clipboard denial/fallback, actual recovery downloads and sticky offset remain intact.
- `node scripts/eng004_browser_check.js`: 46 passed; scope, encoding, clipboard and keyboard focus behavior remain intact.
- `git diff --check`: passed.

Browser commands used `NODE_PATH=/Users/dpartin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules GT_BROWSER_CHANNEL=chrome` and the isolated checkout served at `http://127.0.0.1:8765`. All contexts were fresh and contained synthetic data only. The new blocked-read assertion initially used the wrong status selector; it timed out, was corrected to the actual `#backup-download-status`, and the full suite then passed.

Visually inspected 375px lesson footer and recovery banner plus 1280px recovery layout. Controls fit and content remains reachable. Screenshots were kept temporarily outside Git. No real device, Safari, screen reader, production deployment or cross-device sync was tested. This review does not establish that restore works; it has not been built.

## Integration and next packet

Review correction `77e3a69` was included in PR #19, merged as `caab24d6a2602df02adb533bcab578d0aa17fe48` with GitGuardian green. ENG-006a and REV-004 are DONE; the checkpoint and next packet are in HANDOFF. ENG-006b is cleared to start: strict validation and read-only preview of this versioned format. ENG-006c remains held. Parent ENG-006 remains incomplete. Claude PR #17 requires separate editorial/technical review and Dave's content approval.
