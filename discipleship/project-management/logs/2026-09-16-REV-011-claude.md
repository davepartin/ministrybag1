# REV-011: independent review of ENG-006c confirmed restore
Date: 2026-09-16
Agent / reviewer: Claude (Claude Code), coordinator
Status: DONE (review); ENG-006c integrated with corrections
Branch: cursor/eng-006c-confirmed-restore (PR #27), reviewed at `cfeb419`
Base commit: origin/main `c95e073`, already contained in the PR branch
Scope / files reserved: `scripts/backup-restore.js`, `scripts/test_backup_restore.js`, stale restore wording in `scripts/backup-preview.js`, `scripts/backup-download.js`, `index.html`, `scripts/backup-format.md`, and the two tests that asserted that wording; this log.

## What was reviewed

The full PR diff (7 files, about 2,780 added lines) against the ENG-006c packet in HANDOFF. Scope stayed inside the packet: no lesson JSON, widgets, images, login, Discuss UI or SEC-001b Bible reading code changed. The two edited ENG-006b browser assertions were correctly updated from "no restore exists" to "Restore is present and disabled until confirmed."

Confirmed against the packet by reading the code: only a complete version 1 file that passes the current validator reaches confirmation, partial files stay preview-only; every conflict (including retained ambiguity items) needs an explicit keep-device or use-backup choice and nothing is preselected; the safety download is built from current in-memory state including captured `originalRaw` and must be acknowledged before Restore enables; the confirmation fingerprint covers file identity, memory, persisted raw strings, load reasons and writability and is rechecked at apply time, and storage events, saves and guard changes mark it stale; writes go store by store with verification reads; success replaces memory only after all writes verify, then rereads persisted bytes, clears only recovered guards and reports counts without a persistent green banner. Imported strings are rendered with `textContent`: a synthetic backup value containing an `<img onerror>` payload rendered as inert text at 390 px.

## Defects found and corrected

1. Rollback skipped a store whose write landed but then failed verification. Reproduced with synthetic storage: answers rolled back, but completion kept the new bytes while the result claimed "Original data, memory and write guards were not replaced." Fixed in `applyConfirmedRestore`: after a failure the failed store is reread and rolled back too if it no longer matches the pre-write snapshot. If that store cannot be put back, the result is `rollback-failed` with the safety download offered, never an unchanged claim.
2. The helper skipped the stale check when no fingerprint was passed. The page always passes one, but the helper now refuses a missing fingerprint as `stale-confirmation`.
3. Learner-facing text still said "Applying a backup is not available yet" and "Restore is not available yet" in preview notes and backup download status, and the preview said conflicts "default" to keeping the device value, contradicting the no-preselection rule. Reworded in `backup-preview.js`, `backup-download.js`, `index.html` and `backup-format.md`; the two tests asserting the old wording now assert the new wording and the absence of the old.

Six new unit assertions cover defects 1 and 2 (landed-then-failed write rolled back for every store, persistent failure reports rollback-failed, missing fingerprint refused). Four of them fail on the original `cfeb419` helper and pass after the fix.

## Verification

On the corrected branch (main already included), Node 24.20.0 and Playwright 1.57.0 with `python3 -m http.server`:
- Unit: answer storage 44, backup download 53, backup preview 75, backup restore 94 (was 87), Bible text 415, lesson assets 8, export 40, save feedback 41, widget iframe 30. `bash scripts/qa_foundation.sh` passes.
- Browser: eng001 through eng006b, eng006c, review_eng 14, review_save_recovery 20, sec001b 24 and ux001b all pass.
- Visual: restore review dialog at 390 px inspected; Restore stays disabled before choices and safety acknowledgement.

Checks not run: real iPhone/Safari; a human learner using restore.

## Follow-up, not blocking

The restore review uses technical labels such as `answers:question-101-5-key`, "stores" and "version 1". It is accurate but not learner-friendly. Tracked as UX-004 for plain-language labels (lesson and question names) before a pilot uses restore.

## Handoff

Parent ENG-006 is DONE with ENG-006a, 006b and 006c integrated. Commit / PR / GitHub sync state: corrections pushed to PR #27, then merged to main.
