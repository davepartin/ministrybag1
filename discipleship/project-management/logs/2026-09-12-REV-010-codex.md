# REV-010: ENG-006b correction review

September 12, 2026. Codex coordinator. Status: DONE after integration. Branch `codex/recheck-eng006b`, isolated checkout `/tmp/gt-eng006b-review`; Dave's lesson preview at port 8877 remained untouched. Reviewed PR #20 at `acc9659` and merged current main normally before integration.

The four REV-008 correction groups were present and passed their new checks. The independent edge pass found three related gaps: a retained ambiguity container could omit its required `items`; a stored unknown key named `toString` disappeared from the unknown comparison because the deduplicator inherited object properties; and a read pending before any modal opened survived navigation, opening and cancelling a newer picker, or a picker cancel event.

The review correction requires an own ambiguity `items` object, uses a prototype-free key set, and invalidates pending reads on navigation, series/session changes, export opening, new picker opening and picker cancellation. Tests cover the three cancellation paths, state preservation and the prototype-named key.

## Verification

- `node scripts/test_backup_preview.js`: **75 passed**.
- `bash scripts/qa_foundation.sh`: passed, including the 75 preview checks.
- `scripts/eng006b_browser_check.js`: **89 assertions passed** with Chrome at the configured local URL. Includes actual input behavior, stale success/failure, FileReader fallback, navigation, new picker and cancel invalidation; synthetic learner state stayed unchanged.
- Unit regressions: backup download 53, save feedback 41, answer storage 44, lesson export 40.
- Browser regressions: ENG-006a passed; save/recovery 20 passed; ENG-004 passed.
- `git diff --check`: passed.

No restore/apply action, upload, account change, learner data, lesson edit or deployment. Actual Safari/iPhone, screen reader and full-disk behavior remain untested. ENG-006c stays separate and held until this merge is recorded.
