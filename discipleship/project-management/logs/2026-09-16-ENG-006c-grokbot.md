# ENG-006c: confirmed restore
Date: 2026-09-16
Agent / reviewer: Grokbot / Cursor cloud agent
Status: REVIEW
Branch: cursor/eng-006c-confirmed-restore
Base commit: de8ec7d5 (origin/main tip, includes COORD-003 and PR #24 quiet-save at 64cfa9b)
Scope / files: scripts/backup-restore.js, scripts/test_backup_restore.js, scripts/eng006c_browser_check.js, scripts/qa_foundation.sh step 14, index.html restore controls, this log. Did not edit lesson JSON, widgets, images, login/provider code, Discuss UI, Scripture/ESV fetch, TASKS.md, HANDOFF.md, data/202-08.json, data/202-09.json, or Claude PR #17.

## Changes and decisions

Confirmed restore sits on top of the ENG-006b preview. Only an exact version 1 file that passes the current validator can reach confirmation. Partial source backups stay preview-only.

Conflict review requires an explicit keep-device or use-backup choice for every disagreement, including differing retained ambiguity items. Nothing is preselected. Backup-only keys are added, device-only keys stay, matching values stay, unknown keys stay stored keys, and ambiguous answers stay unassigned.

Before any write, the page builds and triggers a versioned safety backup of current in-memory and device state, including unsaved edits and captured original raw evidence. Restore stays disabled until that download is started and the learner acknowledges it. The page does not claim the browser proved the file was saved.

Confirmation is bound to a fingerprint of the exact file plus current in-memory values, storage raw strings, load reasons and write guards. File change, answer edit, navigation, a new preview, a storage event or a guard/status change makes confirmation stale and requires a fresh preview. A cancel, stale confirmation or failed write leaves original raw bytes, memory and guards unchanged.

The three stores are one transaction. A complete pre-write snapshot is kept. If any write or verification read fails, already-changed stores roll back. If rollback is incomplete, per-store errors stay visible and the pre-restore safety download is offered. Success is never reported in that case. In-memory state and UI update only after every write verifies.

Failed-load guards clear only after their original raw evidence is in the safety download and the confirmed replacement verifies. Restore errors stay visible. Success shows exact restored, kept and conflict counts without a persistent green save banner.

## Verification
Exact command or browser action, result, and evidence path.
All commands run from `discipleship/` unless noted. Browser prefix:

```sh
python3 -m http.server 8765 --bind 127.0.0.1
NODE_PATH=/tmp/gt-playwright/node_modules node scripts/SCRIPT.js
```

Playwright used bundled Chromium. `GT_BROWSER_CHANNEL=chrome` was not set because system Chrome was not installed in this environment.

1. `node scripts/test_backup_restore.js`
   Result: 87 passed.

2. `node scripts/test_backup_download.js`
   Result: 53 passed.

3. `node scripts/test_backup_preview.js`
   Result: 75 passed.

4. `node scripts/test_save_feedback.js`
   Result: 41 passed.

5. `node scripts/test_answer_storage.js`
   Result: 44 passed.

6. `node scripts/test_lesson_export.js`
   Result: 40 passed.

7. `node scripts/test_widget_iframe.js`
   Result: 30 passed.

8. `node scripts/test_lesson_assets.js`
   Result: 8 passed.

9. `bash scripts/qa_foundation.sh`
   Result: passed, including the 87 restore checks as step 14.

10. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/eng006c_browser_check.js`
    Result: 55 passed against `http://127.0.0.1:8765/index.html`. Covered an actual downloaded source file, safety download bytes, conflict choices with nothing preselected, acknowledgement before Restore, stale confirmation after an in-page edit, cancel, download-restore-reload, empty/false/Unicode/unknown keys, repeated restore, keyboard Tab, confirmation wording, 375/390/1280 layout, and failed-load cancel vs verified replacement. Synthetic data and `/tmp` downloads only.

11. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/eng006a_browser_check.js`
    Result: 60 passed.

12. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/eng006b_browser_check.js`
    Result: 87 passed, 2 failed. The two failures are the ENG-006b assertions that restore is not available and that no Restore button exists. Those are now false because ENG-006c adds confirmed restore. Tab wrap, cancel, and unchanged-data checks still pass.

13. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/eng005_browser_check.js`
    Result: 74 passed.

14. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/review_save_recovery_browser_check.js`
    Result: 20 passed.

15. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/eng004_browser_check.js`
    Result: 46 passed.

16. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/eng003_browser_check.js`
    Result: 36 passed.

17. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/eng002_browser_check.js`
    Result: 14 passed.

18. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/eng001_browser_check.js`
    Result: 17 passed.

19. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/review_eng_browser_check.js`
    Result: 14 passed.

20. `NODE_PATH=/tmp/gt-playwright/node_modules node scripts/ux001b_browser_check.js`
    Result: 108 passed.

Checks not run and why:
- Real iPhone/Safari, a physical full storage disk, screen reader and production deployment were not tested.

Known remaining issue: ENG-006b still expects preview-only copy and no Restore button. Independent review should update those two ENG-006b assertions after accepting this packet. Parent ENG-006 stays incomplete.

## Handoff
Changed files: restore helper and unit tests, restore controls in index.html, ENG-006c browser check, foundation QA hook, this log.
Remaining work / next command: independent review. Parent ENG-006 stays incomplete. Do not merge, deploy, or start DATA-001.
Blocker and unblock action, if any: none for this packet besides the two ENG-006b preview-only assertions above.
Commit / PR / GitHub sync state: worker branch `cursor/eng-006c-confirmed-restore`; PR https://github.com/davepartin/ministrybag1/pull/27 awaiting independent review. Find this packet with `git log -- discipleship/project-management/logs/2026-09-16-ENG-006c-grokbot.md` from repository root.
Pastoral approval evidence, if applicable: not required for this software packet.
